# ============================================
# DEVELOPMENT environment (tự động load khi `npm run dev`)
# File này COMMIT vào git — team pull về là chạy được ngay.
# ============================================

# Backend API — BE test server của team PTH (đã chạy sẵn, có data thật)
# Nếu bạn muốn chạy BE local, tạo file .env.local và override biến này
# (.env.local được git ignore nên không ảnh hưởng team)
NEXT_PUBLIC_API_URL=http://160.250.186.97:3000

# Site URL cho metadata/OG tags (URL của Next.js server khi chạy dev)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# App Environment
NEXT_PUBLIC_ENV=development
